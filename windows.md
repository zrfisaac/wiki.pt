# Windows

O Windows é uma família de sistemas operacionais desenvolvida pela Microsoft, projetada para rodar em computadores pessoais (PCs) e outros dispositivos. Ele oferece uma interface gráfica de usuário (GUI) que permite aos usuários interagir com seus dispositivos por meio de elementos visuais, como janelas, ícones, menus e botões.

## Índice

---

### Descrição de Cada Diretório no Windows

| Diretório             | Descrição                                                                                 |
|-----------------------|-------------------------------------------------------------------------------------------|
| `C:\`                | Diretório raiz do disco principal, onde o sistema operacional e os programas padrão são armazenados. |
| `C:\Windows`          | Diretório principal do sistema operacional Windows, contendo arquivos essenciais para o funcionamento do SO. |
| `C:\Windows\System32` | Contém arquivos e executáveis essenciais para o funcionamento central do sistema operacional. |
| `C:\Program Files`    | Diretório padrão para instalação de programas de 64 bits.                                 |
| `C:\Program Files (x86)` | Diretório padrão para instalação de programas de 32 bits em sistemas de 64 bits.          |
| `C:\Users`            | Contém os diretórios de perfil dos usuários, incluindo arquivos pessoais, configurações e itens da área de trabalho. |
| `C:\Users\Public`     | Diretório compartilhado acessível por todos os usuários do sistema.                        |
| `C:\Documents and Settings` (Legado) | Utilizado em versões antigas do Windows para perfis e configurações de usuários (substituído por `C:\Users` em versões modernas). |
| `C:\Temp`             | Diretório de arquivos temporários, usado pelo sistema e por programas para armazenamento temporário. |
| `C:\ProgramData`      | Armazena dados de aplicativos compartilhados por todos os usuários, como configurações e arquivos de configuração. |
| `C:\AppData`          | Contém dados de aplicativos específicos do usuário, divididos em `Local`, `LocalLow` e `Roaming`. |
| `C:\Recycle Bin`      | Armazena arquivos e pastas excluídos, permitindo que sejam restaurados ou removidos permanentemente. |
| `C:\System Volume Information` | Contém pontos de restauração do sistema e cópias sombra do volume (protegido pelo sistema). |
| `C:\Windows\Temp`     | Arquivos temporários utilizados pelo sistema operacional e aplicativos instalados.         |
| `C:\Windows\WinSxS`   | Armazena assemblies lado a lado para compatibilidade com diferentes versões do Windows.    |

